const campgroundsMiddleware = require("./campgrounds");
const reviewsMiddleware = require("./reviews");
const usersMiddleware = require("./users");

module.exports = { campgroundsMiddleware, reviewsMiddleware, usersMiddleware };
