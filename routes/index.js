const campgroundsRoutes = require("./campgrounds");
const reviewRoutes = require("./review");
const userRoutes = require("./users");

module.exports = { campgroundsRoutes, reviewRoutes, userRoutes };
